# BajuStyle 上架工具包 — 新电脑使用指南

本压缩包可在任意 Windows 电脑上架商品、改图、改文案。
核心程序是 **manage.py**（BajuStyle 网站管理器），双击 `启动管理器.bat` 即可运行。

---

## 一、新电脑准备工作（只需做一次）

1. **安装 Python**（必须）
   - 到 https://www.python.org 下载 Python 3.11 或更新版本
   - 安装时**务必勾选 "Add python.exe to PATH"**（最下面那一项）
   - 一直点下一步装完

2. **安装 Git**（只有想在新电脑直接上线才需要；见第三节路径B）
   - 到 https://git-scm.com 下载安装，默认选项一路下一步即可

---

## 二、上架商品（必做）

1. 把压缩包解压到一个文件夹，例如 `D:\BajuStyle`
2. 双击 **`启动管理器.bat`** 打开管理器
3. 左侧「📂 分类」选一个分类（衣服 / 鞋子 / 包包 / 手表 / 饰品）
4. 右侧填商品信息：封面图、名称（中/英/马/越）、价格、款式（子分类）等
5. 点「💾 保存数据」→ 再点顶部「🔄 重新生成 HTML」
6. 商品就排好版了

> 加新款式（如短裤）：在商品表单的「款式」下拉旁点「➕ 款式」即可。
> 改文字（首页大标题、副标题、按钮等）：用 `site_text_template.txt` 填好发给你技术，或直接告诉技术改。

---

## 三、把改动上传到网站（两种选择）

### 路径 A：新电脑只改，原电脑上线（最省事，推荐）
新电脑上架/改图完成后，把整个 `BajuStyle` 文件夹（或只改动的文件：
`products.json`、`images\`、`*.html`）通过 U 盘 / 微信 / 网盘拷回**原来那台电脑**，
在原电脑打开管理器点「🚀 部署上线」即可。

### 路径 B：新电脑直接上线（需装 Git，见第一节第2步）
1. 在 `BajuStyle` 文件夹里，右键 →「Git Bash Here」
2. 执行（把仓库连过来，只需一次）：
   ```
   git clone git@github.com:zjlxd388/bajustyle.git _repo
   ```
   等下载完，把 `_repo` 里的 `.git` 文件夹**剪切**到 `BajuStyle` 根目录，
   然后删掉空的 `_repo` 文件夹。
3. 打开管理器，顶部点「⚙ 设置 / GitHub」，填入：
   - 用户名：`zjlxd388`
   - Token：你的 GitHub Personal Access Token（在 github.com → Settings → Developer settings → Tokens 生成，勾 repo 权限）
   - 点「测试连接」→ 成功后「保存」
4. 之后上架完，直接点「🚀 部署上线」就会自动推送到网站。

> 若新电脑配了 SSH key 并加到 GitHub，可不用 Token，直接 SSH 推送。

---

## 四、可选：一键翻译功能

「🌐 一键翻译」需要 DeepSeek API Key（不填也能手动填四种语言，不影响上架）。
如需启用：在文件夹里新建 `config.json`，内容：
```json
{ "api_key": "你的DeepSeek Key" }
```

---

## 五、文件说明

| 文件 | 作用 |
|---|---|
| manage.py | 网站管理器主程序 |
| 启动管理器.bat | 一键启动管理器 |
| products.json | 所有商品/分类数据 |
| site.js / site.css | 网站前端引擎与样式 |
| *.html | 各页面（首页/分类/详情） |
| images\ | 所有商品图、二维码、主图 |
| import_products.py + 批量导入.bat | 批量导入商品（见 批量导入使用说明.md） |
| site_text_template.txt | 全站文字修改模板 |
| 新电脑部署指南.md | 本文件 |

如遇到问题，联系你的技术（阿东）协助。
